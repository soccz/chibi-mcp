[package]
name = "chibi-mcp-desktop"
version = "0.2.1"
description = "tteoki — Korean rice cake desktop character"
authors = ["soccz"]
license = "MIT"
edition = "2021"

[lib]
name = "chibi_mcp_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true
